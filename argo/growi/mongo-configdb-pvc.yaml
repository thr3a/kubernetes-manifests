apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-configdb
  name: mongo-configdb
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1G
