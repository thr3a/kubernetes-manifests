apiVersion: apps/v1
kind: Deployment
metadata:
  name: httpbin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: httpbin
      # version: v1
  template:
    metadata:
      labels:
        app: httpbin
        # version: v1
    spec:
      serviceAccountName: sa
      containers:
      - image: docker.io/kennethreitz/httpbin
        resources: {}
        imagePullPolicy: IfNotPresent
        name: httpbin
        ports:
        - containerPort: 80
