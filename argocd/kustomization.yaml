apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
- https://raw.githubusercontent.com/argoproj/argo-cd/v2.6.7/manifests/install.yaml
- gateway.yaml
patches:
- insecure-patch.yaml
- resync-patch.yaml
# - 2_4to2_5-patch.yaml

patchesJson6902:
- target:
    version: v1
    kind: Service
    name: argocd-server
  patch: |-
    - op: replace
      path: /spec/ports/1
      value: {"name":"grpc","port":443,"protocol":"TCP","targetPort":8080}


# ports:
#   - name: http
#     port: 80
#     protocol: TCP
#     targetPort: 8080
#   - name: https
#     port: 443
#     protocol: TCP
#     targetPort: 8080
# selector:
